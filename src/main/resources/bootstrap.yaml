server:
  port: 8484
spring:
  profiles:
    active: dev
  application:
    name: aggregatorservice
  cloud:
    config:
      uri: http://localhost:8585/config-server
      name: ${spring.application.name}
      fail-fast: true